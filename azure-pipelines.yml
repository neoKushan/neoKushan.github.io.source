trigger:
- master

pool:
  name: Hosted

steps:
- task: giuliovdev.hugo-extension.hugo-build-task.HugoTask@1
  displayName: 'Hugo site generate'
  inputs:
    destination: '$(Build.ArtifactStagingDirectory)'
    hugoVersion: 0.60.1

- task: PublishBuildArtifacts@1
  displayName: 'Publish Artifact: drop'
